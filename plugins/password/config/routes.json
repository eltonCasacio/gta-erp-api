{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "password.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/",
      "handler": "password.reset",
      "config": {
        "policies": []
      }
    }
  ]
}
